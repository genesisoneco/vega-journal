# Jekyll build output
_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/

# Ruby
Gemfile.lock
vendor/

# Wrangler / Cloudflare local cache & build output
worker/.wrangler/
worker/node_modules/
.dev.vars
# One-time secret drop file (deleted right after the secret is set)
worker/.resend-key
.resend-key

# Market sensor scratch output (regenerated each run, never committed)
tools/market_snapshot.json
tools/market_brief.md
tools/.pipeline-token

# Python
__pycache__/
*.pyc

# Editor / OS
.DS_Store
Thumbs.db
*.swp
